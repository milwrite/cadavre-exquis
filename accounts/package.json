{
  "name": "cail-work-accounts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "npm run types && tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "@cuny-ai-lab/cail-identity": "5.2.5",
    "jose": "6.2.3"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "4.128.0",
    "miniflare": "5.20260831.0-alpha",
    "esbuild": "0.28.1"
  }
}
